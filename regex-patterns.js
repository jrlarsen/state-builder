const patterns = {
   "minus": /^[-]/,
   "plus": /^[+]/,
   "slash": /^[/]/,
   "star-star": /^\*\*/,
   "star": /^[*]/,
   "identifier": /^[a-zA-Z]+[0-9]*(\.[a-zA-Z]+[0-9]*)*/,
   "whitespace": /^\s+/,
   "number": /^[1-9][0-9]*(\.[0-9]+)?/,
   "left-parens": /^\(/,
   "right-parens": /^\)/,
   "left-brace": /^{/,
   "right-brace": /^}/,
   "left-bracket": /^\[/,
   "right-bracket": /^]/,
   "comma": /^,/,
   "caret": /^\^/,
   "colon": /^:/,
   "equal-equal": /^==/,
   "bang-equal": /^!=/,
   "less-equal": /^<=/,
   "less": /^</,
   "greater-equal": /^>=/,
   "greater": /^>/,
   "bang": /^!/,
   "amp-amp": /^&&/,
   "pipe-pipe": /^\|\|/,
};

export default patterns;
